# Shared Utilities

## Files
- `__init__.py`: Package initialization.
- `logging.py`: Structured logging configuration (JSON format for dashboard ingestion).
- `security.py`: HMAC SHA-256 verification, constant-time comparison.
- `helpers.py`: Common helpers (timestamp, UUID, currency formatting).